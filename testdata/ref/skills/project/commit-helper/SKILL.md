---
name: commit-helper
description: Stage and commit the working tree with a message drawn from the diff. Use when the user asks for a commit.
allowed-tools: Bash(git add *) Bash(git commit *) Bash(git status *)
---

# Commit helper

Run `git status`, then `git add`, then `git commit`.
